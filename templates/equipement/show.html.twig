{% extends 'base.html.twig' %}

{% block title %}Equipement
{% endblock %}

{% block body %}
	<h1>Equipement</h1>

	<table class="table">
		<tbody>
			<tr>
				<th>Id</th>
				<td>{{ equipement.id }}</td>
			</tr>
			<tr>
				<th>Name</th>
				<td>{{ equipement.name }}</td>
			</tr>
		</tbody>
	</table>

	<a class="btn btn-primary" href="{{ path('app_equipement_index') }}">Retour</a>

	<a class="btn btn-warning" href="{{ path('app_equipement_edit', {'id': equipement.id}) }}">Editer</a>

	{{ include('equipement/_delete_form.html.twig') }}
{% endblock %}
