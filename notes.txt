Générer des gites: 
(       Coller cette instruction dans Home() dans HomeController
        Lancer l'index du site puis retirer l'instruction de HomeController
)        
        
                // START

                for ($i = 1; $i <= 20; $i++) {
                    $manager = $doctrine->getManager();
        
                    $gite = new Gite();
                    $gite
                        ->setNom("Gite " . $i)
                        ->setDescription("Description Gite " . $i)
                        ->setSurface(random_int(50, 200))
                        ->setChambre(random_int(1, 10))
                        ->setCouchage(random_int(2, 15));
        
                    $manager->persist($gite);
        
                    $manager->flush();
                }
        
                // END




Créer une relation Gite / Use 

php bin/console make:entity

 > User

 > gites
 > relation

 > Gite

 > OneToMany

 >
 >
 >

C:\wamp64\www\gites-progica>php bin/console make:migration

C:\wamp64\www\gites-progica>php bin/console doctrine:migrations:migrate
 >